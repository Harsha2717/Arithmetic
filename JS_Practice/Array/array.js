let values = new Array();
//or
let array = [];
//Empty array we can directly add elements to the Array in the sqaure brackets
//We can also add using push method
console.log(array)

//Adding Elements to Array
array.push(1);
array.push(2);
array.push(3);

console.log(array)

//Fetching Elements using index values

console.log(array[0]);
console.log(array[1]);
console.log(array[2]);
//When we try to fetch the value outside the length it prints undefined
console.log(array[3]);
