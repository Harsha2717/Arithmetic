//Map is used to create a new array or apply some operations to a existing array 
//this is applied to the whole array
let array = [1,2,3,4,5,6];
//console.log(array.map(num => num*2));//Whole array elements are m

//we can apply filter to array and also apply the map
//array.filter(num => num%2===0).map(num => num*2).forEach(num => console.log(num));


