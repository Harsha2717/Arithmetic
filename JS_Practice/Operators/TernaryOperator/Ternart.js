//Ternary Operator is a Special Operator that is single lined if and else
let num = 2;
let result;
// if(num %2 === 0)
//     result = "Even";
// else
// {
//     result = "Odd";
// }

//We can write this code in a single Statement using Ternary
result = (num%2 === 0) ? "Even":"Odd"
console.log(result)